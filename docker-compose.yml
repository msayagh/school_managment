version: '3.8'

# Docker Compose configuration for School Management System
# This file orchestrates all microservices

services:
  # Database Service - MySQL
  database:
    image: mysql:8.0
    container_name: school-database
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-rootpassword}
      MYSQL_DATABASE: school_management
      MYSQL_USER: school_admin
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-changeme}
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./services/database/init:/docker-entrypoint-initdb.d
    networks:
      - school-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${MYSQL_ROOT_PASSWORD:-rootpassword}"]
      interval: 10s
      timeout: 5s
      retries: 5

  # API Gateway Service
  api-gateway:
    build:
      context: .
      dockerfile: services/api-gateway/Dockerfile
    container_name: school-api-gateway
    ports:
      - "8000:8000"
    environment:
      - NODE_ENV=development
      - DB_HOST=database
      - DB_PORT=3306
      - DB_USER=school_admin
      - DB_PASSWORD=${MYSQL_PASSWORD:-changeme}
      - DB_NAME=school_management
      - JWT_SECRET=${JWT_SECRET:-dev-secret-insecure}
      - STUDENTS_SERVICE_URL=http://students-service:3001
      - TEACHERS_SERVICE_URL=http://teachers-service:3002
      - ACTIVITIES_SERVICE_URL=http://activities-service:3003
      - ROOMS_SERVICE_URL=http://rooms-service:3004
      - BOOKINGS_SERVICE_URL=http://bookings-service:3005
    depends_on:
      database:
        condition: service_healthy
      students-service:
        condition: service_started
      teachers-service:
        condition: service_started
      activities-service:
        condition: service_started
      rooms-service:
        condition: service_started
      bookings-service:
        condition: service_started
    networks:
      - school-network

  # Students Microservice
  students-service:
    build:
      context: .
      dockerfile: services/students/Dockerfile
    container_name: school-students-service
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=development
      - DB_HOST=database
      - DB_PORT=3306
      - DB_USER=school_admin
      - DB_PASSWORD=${MYSQL_PASSWORD:-changeme}
      - DB_NAME=school_management
      - SERVICE_PORT=3001
      - JWT_SECRET=${JWT_SECRET:-dev-secret-insecure}
    depends_on:
      database:
        condition: service_healthy
    networks:
      - school-network

  # Teachers Microservice
  teachers-service:
    build:
      context: .
      dockerfile: services/teachers/Dockerfile
    container_name: school-teachers-service
    ports:
      - "3002:3002"
    environment:
      - NODE_ENV=development
      - DB_HOST=database
      - DB_PORT=3306
      - DB_USER=school_admin
      - DB_PASSWORD=${MYSQL_PASSWORD:-changeme}
      - DB_NAME=school_management
      - SERVICE_PORT=3002
      - JWT_SECRET=${JWT_SECRET:-dev-secret-insecure}
    depends_on:
      database:
        condition: service_healthy
    networks:
      - school-network

  # Activities Microservice
  activities-service:
    build:
      context: .
      dockerfile: services/activities/Dockerfile
    container_name: school-activities-service
    ports:
      - "3003:3003"
    environment:
      - NODE_ENV=development
      - DB_HOST=database
      - DB_PORT=3306
      - DB_USER=school_admin
      - DB_PASSWORD=${MYSQL_PASSWORD:-changeme}
      - DB_NAME=school_management
      - SERVICE_PORT=3003
      - JWT_SECRET=${JWT_SECRET:-dev-secret-insecure}
    depends_on:
      database:
        condition: service_healthy
    networks:
      - school-network

  # Rooms Microservice
  rooms-service:
    build:
      context: .
      dockerfile: services/rooms/Dockerfile
    container_name: school-rooms-service
    ports:
      - "3004:3004"
    environment:
      - NODE_ENV=development
      - DB_HOST=database
      - DB_PORT=3306
      - DB_USER=school_admin
      - DB_PASSWORD=${MYSQL_PASSWORD:-changeme}
      - DB_NAME=school_management
      - SERVICE_PORT=3004
      - JWT_SECRET=${JWT_SECRET:-dev-secret-insecure}
    depends_on:
      database:
        condition: service_healthy
    networks:
      - school-network

  # Bookings Microservice
  bookings-service:
    build:
      context: .
      dockerfile: services/bookings/Dockerfile
    container_name: school-bookings-service
    ports:
      - "3005:3005"
    environment:
      - NODE_ENV=development
      - DB_HOST=database
      - DB_PORT=3306
      - DB_USER=school_admin
      - DB_PASSWORD=${MYSQL_PASSWORD:-changeme}
      - DB_NAME=school_management
      - SERVICE_PORT=3005
      - JWT_SECRET=${JWT_SECRET:-dev-secret-insecure}
    depends_on:
      database:
        condition: service_healthy
    networks:
      - school-network

  # UI Service
  ui-service:
    build:
      context: .
      dockerfile: services/ui/Dockerfile
    container_name: school-ui-service
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
      - API_GATEWAY_URL=http://api-gateway:8000
    depends_on:
      - api-gateway
    networks:
      - school-network

networks:
  school-network:
    driver: bridge

volumes:
  mysql_data:
